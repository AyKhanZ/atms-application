import { environment } from '../../../environments/environment';

export const adminApiUrl = `${environment.apiUrl}/admin/api/v1`;
export const projectApiUrl = `${environment.apiUrl}/project/api/v1`;
